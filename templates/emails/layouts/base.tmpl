<!DOCTYPE html>
<html>
<head>
    <title>{{.subject}}</title>
    {{block "style" .}}{{end}}
</head>
<body>
<div class="container">
    <div class="row">
        <div class="col-sm-8">
        <h1>{{.subject}}</h1>
        {{ template "content" . }}
        </div>
    </div>
</div>

</body>
</html>